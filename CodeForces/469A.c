#include<stdio.h>

int main(){

    int n,p,q;
    scanf("%d\n%d\n%d",&n,&p,&q);

    int prr[p];
    int qrr[q];

    for (int i = 0; i < p; i++)
    {
        scanf("%d",&prr[i]);
    }

    for (int j = 0; j < q; j++)
    {
        scanf("%d",&qrr[j]);
    }
    
    int final[n];
    for (int i = 0; i < n; i++)
    {
        final[i] = i + 1;
    }
    for (int i = 0; i < n; i++)
    {
        if (prr[i] == )
        {
            /* code */
        }
        ;
    }

    return 0;
}

